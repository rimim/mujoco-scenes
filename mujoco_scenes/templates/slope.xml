<mujoco model="{name} slope scene">
  <include file="{path}"/>
  <default>
    <geom friction="1.5 0.01 0.0006"/>
  </default>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0" />
    <rgba haze="0.15 0.25 0.35 1" />
    <global azimuth="150" elevation="-10" />
  </visual>

  <statistic center="0 0 .3" extent="0.6"/>

  <asset>
    <!-- Skybox + ground textures -->
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072" />
    <texture type="2d" name="groundplane" builtin="checker" mark="edge"
             rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3" markrgb="0.8 0.8 0.8"
             width="300" height="300" />
    <material name="groundplane" texture="groundplane" texuniform="true"
              texrepeat="5 5" reflectance="0.2" />

    <!-- Heightfield from slope.png -->
    <!-- slope.png must be grayscale, gradient left→right -->
    <!-- size = [sx sy sz sh]: half-length, half-width, max height, skirt -->
    <hfield name="slope_hf" file="slope.png" nrow="64" ncol="256" size="3 1 0.6 0.1"/>
  </asset>

  <worldbody>
    <light pos="0 0 3" dir="0 0 -1" directional="false" />

    <body name="floor">
      <!-- Infinite flat plane -->
      <geom pos="0 0 0" name="floor" size="0 0 .125"
            type="plane" material="groundplane"
            conaffinity="1" contype="1" />

      <!-- Sloped hill (6m x 2m, rises 0.6m along +x) -->
      <geom name="slope" type="hfield" hfield="slope_hf"
            pos="5 0 0"
            material="groundplane" contype="1" conaffinity="1"/>
    </body>
  </worldbody>
</mujoco>
